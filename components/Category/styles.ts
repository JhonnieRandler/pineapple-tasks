import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        backgroundColor: 'rgba(143, 11, 255, 0.5)',
        borderRadius: 10,
        padding: 30
    },
    title: {
        fontFamily: 'Cairo',
        fontWeight: '700',
        fontSize: 20,
        color: '#5600A0'
    },
    content: {
        fontFamily: 'Cairo',
        fontWeight: '400',
        fontSize: 18,
        color: '#5600A0'
    }
  });

export default styles;